{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/wintermute/agorabus/PRD-agorabus-nats-uplink.md",
  "intent_slug": "agorabus-nats-uplink",
  "root_motivation": "agorabus is per-machine (UDS socket); NATS is the fleet transport (leaf per node \u2192 `nats.service` on hub :7422), but nothing connects them \u2014 the wm-busbridge unit is masked and empty, and no bridge repo exists. Merge them: the agorabus daemon optionally connects to the local NATS leaf and relays topics both ways, so `agorabus publish agent.activity \u2026` on carbon is heard by a subscriber on redbaron with no new binary, no new daemon, and no new secrets (the leaf already holds hub credentials).",
  "user_persona": "Joe Yen running multiple concurrent Claude Code sessions on a single Linux laptop (single-user trust model). The CLI is invoked by SessionStart/Stop hooks and by Claude itself during a session for ad-hoc peer queries.",
  "unfakeable_metric": {
    "name": "acceptance_tests_passing_count",
    "lower_is_better": false,
    "harness_command": "scripts/run-metrics.sh",
    "target": null
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "Given no uplink.toml exists, When the daemon starts and the pre-existing test suite runs, Then behavior matches v0.12.0 and all prior tests pass (uplink off by default).",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "Given an uplink.toml with any subset of enabled/url/node, When the daemon parses it, Then present fields are honored and missing ones take the documented defaults; Given a malformed file, When parsed, Then one error is logged and the daemon falls back to uplink-off rather than crashing.",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "Given uplink enabled and a reachable nats-server, When `agorabus doctor` runs, Then its output includes `uplink: connected` and the url.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "MUST",
      "description": "Given uplink enabled and no nats-server listening, When the daemon starts, Then the local bus serves normally and doctor reports `uplink: reconnecting`; no panic, no exit.",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "id": "AC5",
      "level": "MUST",
      "description": "Given a connected uplink, When the nats-server stops and later restarts, Then local pub/sub keeps working throughout and the uplink reconnects within 30s (observed via doctor polling in test).",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "id": "AC6",
      "level": "MUST",
      "description": "Given a connected uplink, When a client publishes on local topic `T`, Then a message arrives on NATS subject `wm.bus.T` wrapped in an envelope containing `origin_node` and the original JSON payload.",
      "test": "tests/acceptance_ac6.rs"
    },
    {
      "id": "AC7",
      "level": "MUST",
      "description": "Given two uplinked daemons A and B on one test nats-server, When a client publishes via A, Then a subscriber attached to B receives the payload exactly once.",
      "test": "tests/acceptance_ac7.rs"
    },
    {
      "id": "AC8",
      "level": "MUST",
      "description": "Given the same two-daemon harness, When A publishes a 10-message burst, Then a subscriber on A receives no uplink echo of A's own messages and B's per-message delivery count stays 1 (no loop amplification).",
      "test": "tests/acceptance_ac8.rs"
    },
    {
      "id": "AC9",
      "level": "MUST",
      "description": "Given both daemons connected, When `agorabus peers` runs on A, Then B's peers appear tagged `node=testb`; When B's daemon is killed, Then those peers disappear within the peer TTL.",
      "test": "tests/acceptance_ac9.rs"
    },
    {
      "id": "AC10",
      "level": "SHOULD",
      "description": "Given remote peers are known, When `agorabus peers --fleet` runs, Then only remote peers are listed; When plain `peers` runs, Then local-peer output is byte-compatible with v0.12.0 apart from the added `node` field.",
      "test": "tests/acceptance_ac10.rs"
    },
    {
      "id": "AC11",
      "level": "MUST",
      "description": "Given any uplink state (disabled, connected, reconnecting), When `agorabus doctor` runs, Then its exit code is unchanged by the uplink state (uplink lines are informational).",
      "test": "tests/acceptance_ac11.rs"
    },
    {
      "id": "AC12",
      "level": "SHOULD",
      "description": "Given `nats-server` is absent from PATH, When the integration tests run, Then they skip with an explicit `skipped: nats-server not on PATH` message instead of passing vacuously; Given it is present, Then the harness spawns a real throwaway server on a random port.",
      "test": "tests/acceptance_ac12.rs"
    },
    {
      "id": "AC13",
      "level": "MUST",
      "description": "Given the finished diff, When `cargo test` and clippy run, Then the version is 0.13.0, the suite is fully green, and clippy is clean on the diff.",
      "test": "tests/acceptance_ac13.rs"
    },
    {
      "id": "AC14",
      "level": "SHOULD",
      "description": "Given the finished build, When the README is read, Then it documents uplink.toml, the `wm.bus.<topic>` subject scheme, the envelope, and the leaf-owns-credentials model.",
      "test": "tests/acceptance_ac14.rs"
    }
  ],
  "scope": [
    "Single multi-subcommand CLI binary `agorabus` (one of: daemon, announce, peers, publish, subscribe, heartbeat).",
    "Unix-domain socket transport with newline-delimited JSON framing.",
    "In-memory peer registry with heartbeat-based liveness; no on-disk persistence in v1.",
    "Pub/sub with dotted-topic prefix subscription matching.",
    "Fail-open client behavior: missing daemon never raises an error from a Claude session.",
    "0600 socket permissions; relies on UDS uid filtering (single-user model)."
  ],
  "non_goals": [
    "Direct hub connections or credential handling inside agorabus; the uplink only ever talks to the local nats-leaf, which owns hub auth (PRD-agorabus-nats-uplink).",
    "Strong consistency or real mutual exclusion (advisory only; real locks belong to `flock`).",
    "Persistent ring-buffer log to ndjson (Phase 1 in the PRD; deferred \u2014 daemon is memory-only in v1).",
    "`send` targeted messages with opt-in receive (PRD Phase 2; deferred).",
    "Sub-agent integration / `Skill(...)` dispatch interception (PRD Phase 3; deferred).",
    "`/self-review` cross-session error aggregation (PRD Phase 4; deferred).",
    "systemd-user-socket activation (one launch mode only in v1: explicit `agorabus daemon`).",
    "TLS, ACLs, per-topic auth, or any non-uid auth beyond UDS file permissions.",
    "Service-target (`--target service`) under autobuilder v1: this is a multi-subcommand CLI where `daemon` is one of several subcommands of the same binary."
  ],
  "hard_constraints": {
    "rust_edition": "2024",
    "target_kind": "cli",
    "deny_unsafe": true,
    "max_deps": 12,
    "msrv": "1.85",
    "additional": {
      "transport": "unix-domain-socket-primary; optional NATS uplink to the local leaf (wm.bus.<topic> relay, origin_node envelope) per PRD-agorabus-nats-uplink -- local UDS protocol unchanged, uplink is opt-in via ~/.config/agorabus/uplink.toml",
      "framing": "newline-delimited-json",
      "default_socket_path": "~/.cache/agorabus/sock",
      "default_heartbeat_timeout_secs": "60",
      "concurrency_model": "tokio-multi-thread"
    }
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Why build a Unix-socket pub/sub bus?",
      "a": "Two concurrent Claude sessions today (PIDs 1202 and 1381) have no way to see each other and risk clobbering shared resources (settings.json, recall DB, MEMORY files)."
    },
    {
      "why": 2,
      "q": "Why does mutual blindness specifically matter (vs. relying on filesystem locks)?",
      "a": "fs locks address the data race but not the semantic race \u2014 two sessions can independently decide to update the same memory in incompatible ways. Need *intent* visibility, not just *write* mutex."
    },
    {
      "why": 3,
      "q": "Why a long-lived bus rather than a watch-and-poll over a shared status file?",
      "a": "Heartbeat + pub/sub gives clean crash detection (no heartbeat for 60s == gone) and pushes events to subscribers in real time; polling a file misses transient publishes and leaves dead-session detection to ad-hoc TTLs."
    },
    {
      "why": 4,
      "q": "Why does this need its own daemon rather than reusing dbus/systemd activation?",
      "a": "Scope is agent-traffic-shaped (announce + topic prefix subscribe + heartbeat liveness), and a ~300 LoC tokio daemon is cheaper to own than the dbus learning curve. Trust model is single-uid; UDS perms suffice."
    },
    {
      "why": 5,
      "q": "Why is fail-open behavior load-bearing?",
      "a": "If a Claude session ever *blocked* on the bus during its hot path, the bus would become a new SPOF for the agent. Fail-open means 'bus down == no peers known == proceed normally,' which keeps the bus advisory rather than authoritative."
    }
  ],
  "created_at": "2026-05-23T00:00:00Z",
  "ambiguities_resolved": [
    {
      "question": "PRD describes both Phase 0 (announce/peers/heartbeat) and Phases 1\u20134 (publish/subscribe, query-state, send, sub-agent integration, self-review). Which phases are in scope for v1?",
      "resolution": "Phase 0 + Phase 1 (publish/subscribe with prefix matching) are MUST. Phases 2\u20134 are explicit non-goals. The MUST-AC set reflects this scope-narrowing."
    },
    {
      "question": "PRD calls this a 'daemon' but autobuilder v1 supports only --target cli/lib. Service target is out of scope.",
      "resolution": "Model as a multi-subcommand CLI where `agorabus daemon` is one subcommand. Same binary serves both client and server roles. This is exactly how the PRD describes the architecture (`agorabus daemon` is mentioned as embedded in the client)."
    },
    {
      "question": "Should the ring-buffer event log (~/.cache/agorabus/log/YYYY-MM-DD.ndjson) be in v1?",
      "resolution": "No. Deferred to a follow-up. v1 daemon is memory-only; replay-on-subscribe is not supported. This keeps the v1 surface within autobuilder's iteration budget."
    },
    {
      "question": "Should `send` targeted messages with opt-in receive be in v1?",
      "resolution": "No. PRD Phase 2. Pub/sub covers the coordination story; targeted send adds an inbox-subscription convention that doesn't pay off until Phase 3 sub-agent traffic exists."
    }
  ]
}
